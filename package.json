{
  "name": "nightframe",
  "private": true,
  "version": "0.1.0",
  "description": "Glojo Group AI — production-ready directory and product site template",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
